import { useTheme } from '@mui/material/styles';

export const useEntryFieldTextStyle = () => {
  return (
    {
      backgroundColor: 'white',
      display: 'flex',
      width: {
        xs: '100%',
      }
    }
  )
}